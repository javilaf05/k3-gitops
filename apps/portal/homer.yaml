apiVersion: v1
kind: ConfigMap
metadata:
  name: homer-config
  namespace: default
data:
  config.yml: |
    title: "Homelab JLC"
    subtitle: "Panel de Control de Infraestructura"
    columns: "3"
    connectivityTest: true
    defaults:
      layout: "columns"
      theme: "default"
    
    services:
      - name: "Infraestructura"
        icon: "fas check-double"
        items:
          - name: "ArgoCD"
            subtitle: "Despliegue GitOps"
            tag: "infra"
            url: "http://10.43.0.1" # Nota: ArgoCD suele estar en otro puerto, si falla, verifica su puerto
            logo: "https://cdn.jsdelivr.net/gh/walkxcode/dashboard-icons@master/png/argo-cd.png"
          - name: "Homer"
            logo: "https://cdn.jsdelivr.net/gh/walkxcode/dashboard-icons@master/png/homer.png"
            subtitle: "Este Dashboard"
            tag: "infra"
            url: "http://10.43.179.1"
      - name: "Servicios"
        icon: "fas network-wired"
        items:
          - name: "Nextcloud"
            logo: "https://cdn.jsdelivr.net/gh/walkxcode/dashboard-icons@master/png/nextcloud.png"
            subtitle: "Nube Privada & Almacenamiento"
            tag: "app"
            url: "http://10.43.209.85"
          - name: "Trilium Notes"
            logo: "https://raw.githubusercontent.com/walkxcode/dashboard-icons/main/png/trilium.png"
            subtitle: "Base de Conocimiento Personal"
            tag: "app"
            url: "http://10.43.106.212"
          - name: "Jellyfin"
            logo: "https://cdn.jsdelivr.net/gh/walkxcode/dashboard-icons@master/png/jellyfin.png"
            subtitle: "Media Server"
            tag: "app"
            url: "http://10.43.125.128:8096"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: homer
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: homer
  template:
    metadata:
      labels:
        app: homer
    spec:
      containers:
      - name: homer
        image: b4bz/homer:latest
        ports:
        - containerPort: 8080
          name: http
        volumeMounts:
        - name: config-volume
          mountPath: /www/assets/config.yml
          subPath: config.yml
      volumes:
      - name: config-volume
        configMap:
          name: homer-config
---
apiVersion: v1
kind: Service
metadata:
  name: homer-service
  namespace: default
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: homer
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: homer-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "traefik"
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
  - host: homer.jlc.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: homer-service
            port:
              number: 80
